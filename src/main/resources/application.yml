server:
  port: 8080
  servlet:
    context-path: /api
  #ssl:
   # enabled: false
    #key-store: classpath:keystore.p12
    #key-store-password: changeit
    #key-store-type: PKCS12
    #key-alias: tomcat

spring:
  application:
    name: legendary
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:mysql://localhost:3306/legendaryuser?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Nackonel1@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

# Custom application properties
app:
  jwt:
    secret: mySecretKey1235678qwertyuiopjhgfdsfghj94gfertyuio2345jds5kjhgfd457654fghfd
    expiration-ms: 86400000
  frontend:
    url: http://localhost:3000
  email:
    from: no-reply@yourapp.com
  security:
    max-login-attempts: 5
    lock-time-duration: 1800000
    otp:
      expiration-minutes: 10
      length: 6
    rate-limit:
      requests-per-hour: 5

# Logging configuration
logging:
  level:
    com.legendaryUser.legendary: DEBUG
    org.springframework.security: DEBUG
    com.legendaryUser.legendary.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
  file:
    name: logs/auth-app.log

  logging:
    level:
      org:
        springframework:
          security: DEBUG
          web: DEBUG
      com:
        myapp: DEBUG

  mail:
    host: smtp.gmail.com
    port: 587
    username: "samuelkunle.sk@gmail.com"
    password: "ktev hzgu dfhf hpmr"  # ⚠️ Use APP PASSWORD, not regular password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connection-timeout: 5000
          timeout: 5000
          write-timeout: 5000
        debug: true  # Optional: for debugging email issues

    logging:
      level:
        org.springframework.mail: DEBUG    # Shows the SMTP conversation
        com.sun.mail: DEBUG                # Shows low-level JavaMail details
        com.zaxxer.hikari: INFO            # (Optional) Reduces DB noise
        org.hibernate: INFO                # (Optional) Reduces DB noise



